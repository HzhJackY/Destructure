# ADR-005 — Unique Capture–Canonical–Merge Flow

Status: FROZEN

The project has one formal production path. Compatibility entry points must call the same owner services and may not implement parallel filtering, Capture, Review, status, Canonical, Merge, or export logic.

Any proposed parallel path requires explicit approval and a superseding ADR.
